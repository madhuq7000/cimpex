# Keep empty. On the live site the API URL is the current domain
# (same host as https://www.amarsavimarsa.com). On localhost the app
# still uses http://localhost:3000 even after a production build.
VITE_SERVER_URL=
